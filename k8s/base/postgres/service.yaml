apiVersion: v1
kind: Service
metadata:
  name: mcp-postgres
  labels:
    app: mcp-postgres
    app.kubernetes.io/name: mcp-postgres
    app.kubernetes.io/component: database
spec:
  clusterIP: None
  selector:
    app: mcp-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
