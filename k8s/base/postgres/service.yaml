apiVersion: v1
kind: Service
metadata:
  name: mcp-postgres
  labels:
    app: mcp-postgres
    app.kubernetes.io/name: mcp-postgres
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: mcp-postgres
