apiVersion: v1
kind: Service
metadata:
  name: mcp-backend
  labels:
    app: mcp-backend
    app.kubernetes.io/name: mcp-backend
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: mcp-backend
