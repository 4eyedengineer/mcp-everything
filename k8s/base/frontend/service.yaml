apiVersion: v1
kind: Service
metadata:
  name: mcp-frontend
  labels:
    app: mcp-frontend
    app.kubernetes.io/name: mcp-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app: mcp-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
