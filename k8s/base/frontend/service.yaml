apiVersion: v1
kind: Service
metadata:
  name: mcp-frontend
  labels:
    app: mcp-frontend
    app.kubernetes.io/name: mcp-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: mcp-frontend
