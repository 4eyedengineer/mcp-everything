{
  "name": "@mcp-everything/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for MCP Everything",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}