# MCP Everything Backend Configuration

# Application
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:4200

# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=mcp_everything
DATABASE_SSL=false
DATABASE_SYNCHRONIZE=true
DATABASE_LOGGING=false

# Anthropic AI
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
ANTHROPIC_MAX_TOKENS=4000
ANTHROPIC_TEMPERATURE=0.3

# GitHub Integration
GITHUB_TOKEN=ghp_your-token-here
GITHUB_BASE_URL=https://api.github.com

# Docker Configuration
DOCKER_HOST=unix:///var/run/docker.sock
DOCKER_REGISTRY_URL=
BUILD_TIMEOUT=300000
MAX_CONCURRENT_BUILDS=3

# JWT Authentication
JWT_SECRET=your-secure-jwt-secret-at-least-32-characters-long
JWT_EXPIRES_IN=24h

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Deployment Configuration
DEFAULT_GIST_VISIBILITY=public
MAX_GIST_SIZE=10485760

# GitHub Container Registry (GHCR)
GHCR_REGISTRY=ghcr.io
GHCR_OWNER=4eyedengineer
GHCR_REPO=mcp-servers

# GitOps Repository Configuration
GITOPS_OWNER=4eyedengineer
GITOPS_REPO=mcp-server-deployments
GITOPS_BRANCH=main

# MCP Server Hosting Domain
MCP_HOSTING_DOMAIN=mcp.example.com
K8S_NAMESPACE=mcp-servers

# ============================================================
# LOCAL DEVELOPMENT (KinD)
# ============================================================
# Set LOCAL_DEV=true to use local KinD cluster instead of cloud
# When enabled:
#   - Uses localhost:5000 registry (no auth)
#   - Writes manifests to k8s/local-gitops/servers/ (not GitHub)
#   - Uses mcp.localhost domain (no TLS)
#
# To set up local K8s environment:
#   ./scripts/kind/setup.sh
#
# LOCAL_DEV=true
# MCP_DOMAIN=mcp.localhost
# CONTAINER_REGISTRY=localhost:5000
# SKIP_TLS=true
# LOCAL_GITOPS_PATH=./k8s/local-gitops/servers
# ============================================================

# Optional: CORS Configuration
# CORS_ORIGINS=http://localhost:4200,https://yourdomain.com